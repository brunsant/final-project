import React from "react";
// import styled from "styled-components";

const Main = () => {
  return (
    <div>
      <header>Here's the main page, where people can start a new retr</header>
      <Main />
    </div>
  );
};

export default Main;
